# 开发上下文

本仓库不在版本库中保存环境与登录细节（避免敏感信息被跟踪）。

请到私有仓库 **ai_context** 阅读其中的 `CONTEXT.md`。

克隆（需本机已配置 GitHub SSH）：

```bash
git clone git@github.com:gugudeshubao/ai_context.git
```

克隆后打开该目录下的 `CONTEXT.md` 即可。
